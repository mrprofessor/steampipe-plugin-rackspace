connection "rackspace" {
  plugin = "rackspace"

  # Rackspace identity endpoint
  # identity_endpoint = "https://identity.api.rackspacecloud.com/v2.0"

  # Tenant ID
  # tenant_id = "<tenant_id>"

  # Access Token for which to use for the API
  # token_id = "<token_id>"

  # Region
  # region = "<region>"
}
